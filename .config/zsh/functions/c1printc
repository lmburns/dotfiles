# @desc: escape code for colors

function printc() {
  local color="%F{$1}"
  echo -E ${(qqqq)${(%)color}}
  print -R ${(qqqq)${(%)color}}
  print -R ${(Vqqqq)${(%)color}}
}

printc "$@"

# vim: ft=zsh:et:sw=0:ts=2:sts=2
