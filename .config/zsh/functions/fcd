# Desc: change directories with fzf

emulate -L zsh

local dir
dir=$(fd ${1:-.} --prune --strip-cwd-prefix --follow -td | lscolors | fzf +m)

if [[ -z "$dir" ]]; then
  zle && zle redisplay
  return 0
fi

builtin cd "$dir"

# vim:ft=zsh:
