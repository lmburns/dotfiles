#compdef mapw

_mapw() {
  local arguments

  arguments=(
     '-h[display this help]'
     '-m[map (show) wid]'
     '-t[toggle wid’s visibility]'
     '-u[unmap (hide) wid]'
     '*:WINID:'
  )

  _arguments -s $arguments
}

_mapw "$@"
