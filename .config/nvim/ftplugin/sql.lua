local Abbr = require("abbr")

Abbr:new("i", "as", "AS", {buffer = true, only_start = false})
Abbr:new("i", "by", "BY", {buffer = true, only_start = false})
Abbr:new("i", "cascade", "CASCADE", {buffer = true, only_start = false})
Abbr:new("i", "create", "CREATE", {buffer = true, only_start = false})
Abbr:new("i", "delete", "DELETE", {buffer = true, only_start = false})
Abbr:new("i", "drop", "DROP", {buffer = true, only_start = false})
Abbr:new("i", "else", "ELSE", {buffer = true, only_start = false})
Abbr:new("i", "end", "END", {buffer = true, only_start = false})
Abbr:new("i", "foreign", "FOREIGN", {buffer = true, only_start = false})
Abbr:new("i", "from", "FROM", {buffer = true, only_start = false})
Abbr:new("i", "group", "GROUP", {buffer = true, only_start = false})
Abbr:new("i", "having", "HAVING", {buffer = true, only_start = false})
Abbr:new("i", "index", "INDEX", {buffer = true, only_start = false})
Abbr:new("i", "insert", "INSERT", {buffer = true, only_start = false})
Abbr:new("i", "into", "INTO", {buffer = true, only_start = false})
Abbr:new("i", "join", "JOIN", {buffer = true, only_start = false})
Abbr:new("i", "key", "KEY", {buffer = true, only_start = false})
Abbr:new("i", "limit", "LIMIT", {buffer = true, only_start = false})
Abbr:new("i", "offset", "OFFSET", {buffer = true, only_start = false})
Abbr:new("i", "on", "ON", {buffer = true, only_start = false})
Abbr:new("i", "order", "ORDER", {buffer = true, only_start = false})
Abbr:new("i", "primary", "PRIMARY", {buffer = true, only_start = false})
Abbr:new("i", "references", "REFERENCES", {buffer = true, only_start = false})
Abbr:new("i", "select", "SELECT", {buffer = true, only_start = false})
Abbr:new("i", "set", "SET", {buffer = true, only_start = false})
Abbr:new("i", "table", "TABLE", {buffer = true, only_start = false})
Abbr:new("i", "then", "THEN", {buffer = true, only_start = false})
Abbr:new("i", "union", "UNION", {buffer = true, only_start = false})
Abbr:new("i", "unique", "UNIQUE", {buffer = true, only_start = false})
Abbr:new("i", "update", "UPDATE", {buffer = true, only_start = false})
Abbr:new("i", "values", "VALUES", {buffer = true, only_start = false})
Abbr:new("i", "when", "WHEN", {buffer = true, only_start = false})
Abbr:new("i", "where", "WHERE", {buffer = true, only_start = false})

--  ══════════════════════════════════════════════════════════════════════

-- local ex = require("nvim").ex

-- ex.iabbrev {"<buffer>", "as", "AS"}
-- ex.iabbrev {"<buffer>", "by", "BY"}
-- ex.iabbrev {"<buffer>", "cascade", "CASCADE"}
-- ex.iabbrev {"<buffer>", "create", "CREATE"}
-- ex.iabbrev {"<buffer>", "delete", "DELETE"}
-- ex.iabbrev {"<buffer>", "drop", "DROP"}
-- ex.iabbrev {"<buffer>", "else", "ELSE"}
-- ex.iabbrev {"<buffer>", "end", "END"}
-- ex.iabbrev {"<buffer>", "foreign", "FOREIGN"}
-- ex.iabbrev {"<buffer>", "from", "FROM"}
-- ex.iabbrev {"<buffer>", "group", "GROUP"}
-- ex.iabbrev {"<buffer>", "having", "HAVING"}
-- ex.iabbrev {"<buffer>", "index", "INDEX"}
-- ex.iabbrev {"<buffer>", "insert", "INSERT"}
-- ex.iabbrev {"<buffer>", "into", "INTO"}
-- ex.iabbrev {"<buffer>", "join", "JOIN"}
-- ex.iabbrev {"<buffer>", "key", "KEY"}
-- ex.iabbrev {"<buffer>", "limit", "LIMIT"}
-- ex.iabbrev {"<buffer>", "offset", "OFFSET"}
-- ex.iabbrev {"<buffer>", "on", "ON"}
-- ex.iabbrev {"<buffer>", "order", "ORDER"}
-- ex.iabbrev {"<buffer>", "primary", "PRIMARY"}
-- ex.iabbrev {"<buffer>", "references", "REFERENCES"}
-- ex.iabbrev {"<buffer>", "select", "SELECT"}
-- ex.iabbrev {"<buffer>", "set", "SET"}
-- ex.iabbrev {"<buffer>", "table", "TABLE"}
-- ex.iabbrev {"<buffer>", "then", "THEN"}
-- ex.iabbrev {"<buffer>", "union", "UNION"}
-- ex.iabbrev {"<buffer>", "unique", "UNIQUE"}
-- ex.iabbrev {"<buffer>", "update", "UPDATE"}
-- ex.iabbrev {"<buffer>", "values", "VALUES"}
-- ex.iabbrev {"<buffer>", "when", "WHEN"}
-- ex.iabbrev {"<buffer>", "where", "WHERE"}
